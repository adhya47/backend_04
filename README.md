# backend_04
Implemented secure auth with Node.js, JWT, and Redis. Added logout using Redis token blacklisting with TTL. Learned Redis deeply—token expiry, key storage, and improving authentication.
